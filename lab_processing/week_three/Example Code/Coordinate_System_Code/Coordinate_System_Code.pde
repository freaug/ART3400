void setup(){
  size(400, 400);
  
  rectMode(CENTER);
  
  /*
  Rectangles 
  Use a template like this to plug in your values to the function
  The values are taken from our drawing
  rect(x position, y position, width, height):
  rect(200       , 200       , 100  , 50    );  
  */
  
  // Uncomment the below code to see a rectangle
  //rect(200, 200, 100, 50);
  
  /*
  Circles
  Use a template like this to plug in your values to the function
  The values are taken from our drawing
  circle(x position, y position, diameter);
  circle(200       , 200       , 100     );
  */
  
  // Uncomment the below code to see a circle
  //circle(200, 200, 100);
  
  /*
  Triangles
  Use a template like this to plug in your values to the function  
  The values are taken from our drawing
  triangle(x1 position, y1 position, x2 position, y2 position, x3 position, y3 position);
  triangle(100        , 300        , 200        , 100        , 300        , 300        );
  */
  
  // Uncomment the code below to see a triangle
  //triangle(100, 300, 200, 100, 300, 300);
  
  /*
  Lines
  Use a template like this to plug in your values to the function  
  The values are taken from our drawing
  line(x1 position, y1 position, x2 position, y2 position);
  line(100        , 300        , 300        , 100        );
  */
  
  // Uncomment the code below to see a line
  //line(100, 300, 300, 100);
  
}
